<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:vm="clr-namespace:ES.Tools.TestApp.ViewModel">

  <DataTemplate DataType="{x:Type vm:ColorToStringConverterViewModel}">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition />
      </Grid.ColumnDefinitions>
      <TextBox Text="{Binding ColorText}" />
      <Ellipse Grid.Column="1">
        <Ellipse.Fill>
          <SolidColorBrush Color="{Binding ColorText, Converter={StaticResource ColorToStringConverter}}" />
        </Ellipse.Fill>
      </Ellipse>
    </Grid>
  </DataTemplate>

</ResourceDictionary>