<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:converters="clr-namespace:ES.WPF.Converters.ES.WPF.Converters;assembly=ES.WPF" xmlns:vm="clr-namespace:ES.Tools.TestApp.ViewModel">

  <converters:ColorToStringConverter x:Key="ColorToStringConverter" />

  <DataTemplate DataType="{x:Type vm:ColorToStringConverterViewModel}">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition />
      </Grid.ColumnDefinitions>
      <TextBox Text="{Binding ColorText}" />
      <Ellipse Grid.Column="1" Margin="10,0">
        <Ellipse.Fill>
          <SolidColorBrush Color="{Binding ColorText, Converter={StaticResource ColorToStringConverter}}" />
        </Ellipse.Fill>
      </Ellipse>
    </Grid>
  </DataTemplate>

</ResourceDictionary>